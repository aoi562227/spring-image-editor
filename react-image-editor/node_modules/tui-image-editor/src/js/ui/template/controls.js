import { getHelpMenuBarPosition } from '@/util';

export default ({ locale, biImage, signUpButtonStyle, loginButtonStyle, loadButtonStyle, downloadButtonStyle, menuBarPosition }) => `
    <ul class="tui-image-editor-help-menu ${getHelpMenuBarPosition(menuBarPosition)}"></ul>
    <div class="tui-image-editor-controls">
        <div class="tui-image-editor-controls-logo">
            <img src="${biImage}" />
        </div>
        <ul class="tui-image-editor-menu"></ul>

        <div class="tui-image-editor-controls-buttons">
            <button class="tui-image-editor-signUp-btn" style="${signUpButtonStyle}">
                ${locale.localize('Sign Up')}
            </button>
            <button class="tui-image-editor-login-btn" style="${loginButtonStyle}">
                ${locale.localize('Login')}
            </button>
            <div style="${loadButtonStyle}">
                ${locale.localize('Load')}
                <input type="file" class="tui-image-editor-load-btn" />
            </div>
            <button class="tui-image-editor-download-btn" style="${downloadButtonStyle}">
                ${locale.localize('Download')}
            </button>
        </div>
    </div>
`;
